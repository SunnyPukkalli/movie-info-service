apiVersion: v1
kind: Service
metadata:
  name: movie-info-service
  labels:
    name: movie-info-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: movie-info
  type: LoadBalancer
